{
  "name": "smart-practice-root",
  "version": "1.0.0",
  "description": "Root package for Smart Practice project",
  "scripts": {
    "dev": "docker-compose up --build",
    "deploy": "echo 'Deployment handled by Render via render.yaml'"
  }
}